import 'package:flutter/material.dart';
import 'package:get/get.dart';

class StudyMainScreen extends StatelessWidget {
  const StudyMainScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
